// Representative shape only — verify against the live {{Provider}} API before production use.
export const {{provider}}{{capability}}Fixture = {
  // realistic common-case sample payload
};

export const {{provider}}{{capability}}EdgeCaseFixture = {
  // e.g. partial refund, split shipment, multi-currency
};

export const {{provider}}{{capability}}MalformedFixture = {
  // missing/null fields the mapper must handle without throwing
};
