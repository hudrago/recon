{
  "name": "recon",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev:api": "pnpm --filter @recon/api run dev",
    "dev:web": "pnpm --filter @recon/web run dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && pnpm --filter @recon/web run typecheck",
    "lint": "eslint . --max-warnings=0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "eslint": "^10.10.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.70.0",
    "vitest": "^2.1.0"
  }
}
